import 'package:flutter/material.dart';

class StripeConnectRedirectPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Stripe Connect registration succedeed"),
      ),
    );
  }
}